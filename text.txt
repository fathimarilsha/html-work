git init
git config --global user.name 'username'
git config --global user.email 'email'
git config list
git status
git add -A /
git commit -m 'messages'
git remote add origin ''
git push origin master

local stystem   local repository    remote repository(github)